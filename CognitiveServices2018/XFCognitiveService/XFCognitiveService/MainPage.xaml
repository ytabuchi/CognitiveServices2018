<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XFCognitiveService"
             x:Class="XFCognitiveService.MainPage"
             Title="MainPage">
    <ScrollView>
        <StackLayout Padding="10">
            <Button Text="Take a picture"
                    Clicked="PictureButton_Clicked" />
            <Image x:Name="image"
                   Aspect="AspectFill" />
            <Button Text="Detect Face"
                    Clicked="FaceButton_Clicked" />
            <StackLayout Orientation="Horizontal">
                <Label Text="OCR printed(off) or handwriting(on)"
                       HorizontalOptions="EndAndExpand"
                       VerticalTextAlignment="Center" />
                <Switch x:Name="ocrSwitch" />
            </StackLayout>
            <Button Text="OCR Text"
                    Clicked="OcrButton_Clicked" />
        </StackLayout>
    </ScrollView>
</ContentPage>